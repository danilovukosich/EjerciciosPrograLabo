namespace Manejador
{
    /*
    Crear un nuevo archivo nombrado cómo ./manejador.ts que, dentro del Namespace
Manejador posea una función.
La función ‘exportable’ CrearAlumno():void deberá obtener por el atributo id
(del correspondiente elemento HTML) los valores del apellido, nombre y legajo
ingresados.

Luego, procederá a crear un objeto de tipo Alumno y lo mostrará:
● por consola
● por alert

Generar documento HTML (./alumno.html) para ingresar los datos del alumno.
Se necesitará tener:
● inputs
● button (asociar el atributo onclick a la función CrearAlumno)
    */

}